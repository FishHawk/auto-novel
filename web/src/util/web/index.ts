import { tryTranslateKeyword } from './keyword';
import { parseUrl, buildNovelUrl, buildChapterUrl } from './url';

export const WebUtil = {
  tryTranslateKeyword,
  parseUrl,
  buildNovelUrl,
  buildChapterUrl,
};
